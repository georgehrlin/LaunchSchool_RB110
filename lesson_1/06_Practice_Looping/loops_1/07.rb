count = 1

until count > 10
  puts count
  count += 1
end

# The `until` loop is the opposite of the `while` loop.
# `while` iterates until the condition evaluates to `fasle`.
# `until` iterates until the condition evaluates to `true`.